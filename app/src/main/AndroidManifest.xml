<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="MissingApplicationIcon">

    <!-- Needed for UDP socket -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".F1RaceHUDApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.F1RaceHUD">

        <!-- HomeActivity is now the launcher -->
        <activity
            android:name=".HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.F1RaceHUD"
            android:windowFullscreen="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- MainActivity (your old launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.F1RaceHUD"
            android:screenOrientation="portrait" />

        <!-- GraphicsActivity (telemetry dashboard) -->
        <activity
            android:name=".GraphicsActivity"
            android:exported="true"
            android:theme="@style/Theme.F1RaceHUD"
            android:screenOrientation="landscape"
            android:windowFullscreen="true" />

    </application>
</manifest>